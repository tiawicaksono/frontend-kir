import {
  GridIcon,
  ListIcon,
  TableIcon,
  PageIcon,
    ArrowDownIcon,
} from "@/icons";

export const menuIconMap: Record<string, string> = {
  dashboard: GridIcon.src,
  pendaftaran: ListIcon.src,
  laporan: TableIcon.src,
  "master-data": PageIcon.src,
  ChevronDown: ArrowDownIcon.src,
};
